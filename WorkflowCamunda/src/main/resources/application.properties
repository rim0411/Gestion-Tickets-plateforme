## PostgreSQL
spring.datasource.url=jdbc:postgresql://localhost:5432/GestionTi
spring.datasource.username=postgres
spring.datasource.password=admin
spring.datasource.driver-class-name=org.postgresql.Driver
spring.main.allow-bean-definition-overriding=true


#drop n create table again, good for testing, comment this in production
spring.jpa.hibernate.ddl-auto=update

camunda.bpm.database.type=postgres


 camunda.bpm.authorization.enabled= true
 

   camunda.bpm.admin-user.id: support
  camunda.bpm.admin-user.password: support
    camunda.bpm.admin-user.firstName: support
 
    camunda.bpm.filter.create: All tasks


spring.mail.host = smtp.gmail.com

#sender's email & password
spring.mail.username = rim.zouary@gmail.com	 
spring.mail.password = rimrami123

#Below Properties are needed for maintaining the session.
#Two Step verification should not be enabled for gmail.

spring.mail.properties.mail.smtp.auth = true
spring.mail.properties.mail.smtp.socketFactory.port = 465
spring.mail.properties.mail.smtp.socketFactory.class = javax.net.ssl.SSLSocketFactory
spring.mail.properties.mail.smtp.socketFactory.fallback = false

# Service name
#spring.application.name=camunda-service
# Port
server.port=8300
# Eureka server url
#eureka.client.service-url.default-zone=http://localhost:8761/eureka
## PostgreSQL
